╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║          🚨 ACTION REQUIRED: Create Database Table (2 Minutes!)             ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│ CURRENT ERROR:                                                               │
│ Error: relation "charging_sessions" does not exist                           │
│                                                                               │
│ WHAT THIS MEANS:                                                             │
│ ✅ Database connection: WORKING                                              │
│ ❌ Database table: MISSING                                                   │
│                                                                               │
│ SOLUTION: Run SQL schema in Neon (one-time, 2-minute task)                  │
└──────────────────────────────────────────────────────────────────────────────┘

╔══════════════════════════════════════════════════════════════════════════════╗
║                          QUICK FIX (7 STEPS)                                 ║
╚══════════════════════════════════════════════════════════════════════════════╝

  1. Open https://console.neon.tech in your browser

  2. Click on your project

  3. Click "SQL Editor" in the left sidebar

  4. Copy the SQL below (select all and copy)

  5. Paste into the SQL Editor

  6. Click "Run" button (or press Cmd+Enter / Ctrl+Enter)

  7. See "CREATE TABLE" success message → Done! ✅

┌──────────────────────────────────────────────────────────────────────────────┐
│ SQL TO RUN (copy everything between the dashed lines):                      │
└──────────────────────────────────────────────────────────────────────────────┘

-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS charging_sessions (
  id VARCHAR(255) PRIMARY KEY,
  date TIMESTAMP NOT NULL,
  energy_added DECIMAL(10, 2) NOT NULL,
  start_soc INTEGER,
  end_soc INTEGER,
  tariff_rate DECIMAL(10, 4),
  cost DECIMAL(10, 2),
  duration INTEGER,
  notes TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  source VARCHAR(50) DEFAULT 'manual',
  octopus_session_id VARCHAR(255)
);

CREATE INDEX IF NOT EXISTS idx_charging_sessions_date 
  ON charging_sessions(date);

CREATE INDEX IF NOT EXISTS idx_charging_sessions_created_at 
  ON charging_sessions(created_at);

CREATE UNIQUE INDEX IF NOT EXISTS idx_charging_sessions_octopus_id 
  ON charging_sessions(octopus_session_id) 
  WHERE octopus_session_id IS NOT NULL;
-- ----------------------------------------------------------------------------

┌──────────────────────────────────────────────────────────────────────────────┐
│ AFTER RUNNING SQL:                                                           │
├──────────────────────────────────────────────────────────────────────────────┤
│ ✅ Vercel deployment will work immediately                                   │
│ ✅ Dashboard will load successfully                                          │
│ ✅ You can add charging sessions                                             │
│ ✅ You can import from Octopus Energy                                        │
│ ✅ Statistics will calculate                                                 │
│                                                                               │
│ Vercel logs will show:                                                       │
│   Connected to Neon Postgres database                                        │
│   GET /api/sessions 200                                                      │
│   GET /api/stats 200                                                         │
└──────────────────────────────────────────────────────────────────────────────┘

╔══════════════════════════════════════════════════════════════════════════════╗
║                          NEED MORE HELP?                                     ║
╚══════════════════════════════════════════════════════════════════════════════╝

📖 Detailed guide: SETUP_DATABASE.md
🔧 Troubleshooting: TROUBLESHOOTING.md
⚡ Quick start: QUICKSTART.md

┌──────────────────────────────────────────────────────────────────────────────┐
│ TIME ESTIMATE: 2 minutes                                                     │
│ ONE-TIME TASK: Once created, table exists permanently                       │
│ DIFFICULTY: Easy - just copy, paste, and click Run                          │
└──────────────────────────────────────────────────────────────────────────────┘

You're almost done! This is the last step to a fully working EV charging tracker! 🚗⚡🎉
